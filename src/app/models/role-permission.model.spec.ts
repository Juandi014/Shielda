import { RolePermissionModel } from './role-permission.model';

describe('RolePermissionModel', () => {
  it('should create an instance', () => {
    expect(new RolePermissionModel()).toBeTruthy();
  });
});
